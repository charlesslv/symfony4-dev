{% extends 'base.html.twig' %}

{% block title %}New Album{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-12">
            <h1>Create new Album</h1>

            {{ include('album/form.html.twig') }}

            <a href="{{ path('album_index') }}" class="btn btn-sm btn-outline-primary">back to list</a>
        </div>
    </div>
{% endblock %}
