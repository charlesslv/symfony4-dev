{% extends 'base.html.twig' %}

{% block title %}{{ 'New Album'|trans }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-12">
            <h1>{{ 'Add new Album'|trans }}</h1>

            {{ include('album/form.html.twig') }}

            <a href="{{ path('album_index') }}" class="btn btn-sm btn-outline-primary">{{ 'Back to list'|trans }}</a>
        </div>
    </div>
{% endblock %}
