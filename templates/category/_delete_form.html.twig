<form method="post" action="{{ path('category_delete', {'id': category.id}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token(category.id) }}">
    <div class="alert alert-danger" role="alert">
        {{ 'Are you sure to delete'|trans }} '{{ category.name }}' ?
    </div>
    <a href="{{ path('category_index') }}" class="btn btn-sm btn-outline-secondary">{{ 'Cancel'|trans }}</a>
    <button class="btn btn-sm btn-outline-danger">{{ 'Delete'|trans }}</button>
</form>
